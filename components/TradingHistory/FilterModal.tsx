import { FilterModalProps } from '@/constants/types'
import { Modal, View } from 'react-native'

const FilterModal = ({ showModal, updateShowModal }: FilterModalProps) => {
  return (
    <Modal>
      <View>
        {/* full screen modal? Make it look kinda like bottomsheet???? look on Pinterest for inspo */}
      </View>
    </Modal>
  )
}

export default FilterModal